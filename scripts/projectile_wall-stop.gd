extends Area

# class member variables go here, for example:
# var a = 2
# var b = "textvar"

func _ready():
	# Called when the node is added to the scene for the first time.
	# Initialization here
	#set_process(true)
	pass

#func _process(delta):
#	# Called every frame. Delta is time since last frame.
#	# Update game logic here.
#	pass
#func _process(delta):
#	move_and_slide(Vector3(0,0,0),Vector3(0,1,0), 0.1,0.785398)
#	print( is_on_wall() )
#	print( is_on_ceiling() )
#	print( is_on_floor() )